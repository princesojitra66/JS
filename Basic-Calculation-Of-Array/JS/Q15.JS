    const inputArray = ['5', 32, 'Daniel'];

    // Convert array to string
    const outputString = inputArray.toString();

    // Display results
    document.getElementById("arrayInput").textContent = JSON.stringify(inputArray);
    document.getElementById("stringOutput").textContent = outputString;